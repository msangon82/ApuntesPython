git add ficheros--------------> Añade ficheros al commit (punto de guardado)

git commit -m "descripción"  -------------> Crea el punto de guardado

git push origin main ---------------------> Envía el commit local al repositorio remoto tambien se puede poner origen y destino.

git pull  -------------------------> Trae el commit remoto al repo local 

git init -------------------------> iniciar un repositorio en git 

git status ----------------------> muestra como estoy 

git log -------------------------> muestra los commits 

git reset  nombredelcommit --hard 



1. Primero se ejecuta git init 
2. Segundo añadir los archiros al commit (git add . (con el punto se guardan todos los archivos))
